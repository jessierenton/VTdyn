Code for Voronoi Tessellation model

- To generate data on fixation probabilities:
  	- for decoupled update run multirun_constant_N.py with the following 		command line arguments:
			av 0 10 X Y Z ...
	
	where X/Y/Z are values of b up to 2 decimal places
	
	- for death-birth update run multirun_constant_N.py with same command 		line args

  Output saved in VTpd_av_decoupled/fixX (X=b) for decoupled update
 		   VTpd_av_db/fixX for death-birth update
				  
  Data in form #fixed #lost #incomplete (sum=1000)